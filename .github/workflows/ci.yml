name: Continuous Integration

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test-javascript:
    name: JavaScript Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        id: setup-node
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install Dependencies
        id: npm-ci
        run: npm ci

      - name: Check Format
        id: npm-format-check
        run: npm run format:check

      - name: Lint
        id: npm-lint
        run: npm run lint

      - name: Test
        id: npm-ci-test
        run: npm run ci-test

  test-action:
    name: GitHub Actions Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v6

      - name: Init Test Git Repository
        id: init-git
        run: |
          cd __fixtures__/workspace
          git init
          git config user.email "test@example.com"
          git config user.name "Test User"
          git add .
          git commit -m "Initial workspace commit"

      - name: Test Action (No Modifications)
        id: test-none
        uses: ./
        with:
          workspace-root: __fixtures__/workspace
          test-all: 'false'
          verbose: 'false'

      - name: Verify No Modifications Detected
        run: |
          echo "Test Count: ${{ steps.test-none.outputs.test-count }}"
          echo "Passed: ${{ steps.test-none.outputs.passed-count }}"
          echo "Failed: ${{ steps.test-none.outputs.failed-count }}"

      - name: Modify a Crate File
        run: |
          echo "// Modified" >> __fixtures__/workspace/crates/add/src/lib.rs
          cd __fixtures__/workspace
          git add crates/add/src/lib.rs
          git commit -m "Modify add crate"

      - name: Test Action (Modified Crate)
        id: test-modified
        uses: ./
        with:
          workspace-root: __fixtures__/workspace
          base-ref: HEAD~1
          head-ref: HEAD
          verbose: 'true'

      - name: Verify Modified Crate Detected
        run: |
          echo "Test Count: ${{ steps.test-modified.outputs.test-count }}"
          echo "Passed: ${{ steps.test-modified.outputs.passed-count }}"
          echo "Failed: ${{ steps.test-modified.outputs.failed-count }}"
